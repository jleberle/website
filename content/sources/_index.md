---
title: "Sources"
# The taxonomy's own listing page is suppressed: /reading/ already lists every
# source, grouped and dated, and two indexes of the same 46 books would just
# compete. Individual term pages (/sources/<slug>/) still render — `build` is
# set here rather than under `cascade` precisely so it does not reach them.
build:
  render: never
  list: never
---
