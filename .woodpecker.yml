when:
  - event: cron
  - event: manual

steps:
  - name: build
    image: hugomods/hugo:exts
    commands:
      - hugo --minify

  - name: link-check
    image: lycheeverse/lychee:latest
    commands:
      - lychee --no-progress --root-dir "$CI_WORKSPACE/public" --config lychee.toml public/
